- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."participants"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "first_name" text NOT NULL, "last_name" text NOT NULL, PRIMARY KEY ("id") ,
      UNIQUE ("id"));
  type: run_sql
- args:
    name: participants
    schema: public
  type: add_existing_table_or_view
